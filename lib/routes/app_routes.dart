// lib/routes/app_routes.dart

/// Defines all route names used throughout the application.
///
/// Centralizing route identifiers helps avoid typos and makes
/// navigation calls easier to maintain and refactor.
abstract class AppRoutes {
  /// Route for the login page (Get.toNamed(AppRoutes.LOGIN))
  static const String login = '/login';
}
